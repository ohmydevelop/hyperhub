布局调整
正则
    进程
    文件
    网络
进程
    规则
网关
    基础
    代理
    凭证
    审计
    路由
    证书
沙盒
    网络
    命令
    文件
进程正则
环境变量

正则规则布局重新设计，采用集合的方式，比如进程规则进入之后显示规则集列表，进入每个规则集列表可以看到具体的规则，支持 space 启用禁用
正则/命令/集合1
    ID
    引用
    [ ] xxx 正则
    [ ] xxx 正则
    [ ] xxx 正则
正则
    命令（命令和进程统一合并）
    文件
    网络
    HTTP 路径

其它配置可以选择多个正则集合，比如进程/规则中
    ID
    启用
    动作
    优先级
    命令行：enter进入列表。直接列出所有的命令正则集，可通过 space 激活不同的规则集


其它举一反三也类似设计


重新设计 TUI
删除正则及其相关，可以考虑git回退撤销
简化配置
路由删除进程和网络配置


沙盒/网络为例
    一条规则内部可以配置多条
    ID
    启用
    动作
    优先级
    按a添加目标和端口

沙盒中的其它也类似，都改成一条规则下面可以配置多个目标，不需要在沙盒中限制只对某个进程生效的配置选项
