# 2026-09-22

## CI 隔离实例测试缺少 CLI 可执行文件

- 现象：`cargo test --workspace --all-targets` 只生成带哈希的测试程序，后续多 Home 集成测试找不到 `target/debug/hyperhub`。
- 修复：Linux CI 在运行隔离实例测试前显式执行 `cargo build -p hyperhub --locked`。
