# 配置界面补充 Firewall 管理

- 在 `hyperhub config` 中增加“防火墙”分类，可管理 Agent Firewall 总开关和可选默认动作。
- 支持新增、删除、启停和编辑 Firewall 规则，包括 ID、优先级、pass/deny、域名或直接 IP/CIDR、端口及进程正则。
- 新规则默认停用，避免尚未填写匹配条件时使配置失效；未配置默认动作仍保持 pass。
- 将“防火墙”分类放在“证书”之后，移除 Firewall 总开关前的复选框；首页仅使用 Space 直接切换。
