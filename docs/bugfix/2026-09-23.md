# Bugfix 2026-09-23

- 智能防护命中大量正常子进程、文件或网络动作时，Jev `pass` 结果会逐条写入普通审计日志，造成无意义的高频增长。
- 修复：非 Debug 模式过滤智能防护 `pass` 详细事件；`deny` 和错误事件始终保留，Debug 模式恢复 `pass` 事件输出。
- 子进程、文件和网络智能判定审计补充完整但已脱敏的 `argv_redacted`，避免记录原始命令行敏感值。
