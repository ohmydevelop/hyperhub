# 2026-09-22

## 隔离运行实例

- 新增 `HYPERHUB_CONTROL_ENDPOINT`，允许测试实例使用独立的本地控制 socket。
- 与独立 `HOME`、`XDG_RUNTIME_DIR` 配合时，配置、日志、Skill 和 Serve 控制面均不再复用默认用户实例。
- Linux、Windows 和其他 Unix 平台的控制端点解析都支持该覆盖变量；Unix 控制目录仍执行所有权和权限校验。
