# 开发原则

- 修改前先理解需求、相关代码、配置、文档及仓库约定。
- 保持改动最小且聚焦，沿用现有结构与风格，不顺带重构无关代码。
- 保留用户已有修改，不得覆盖、回退或提交与当前任务无关的变更。
- 未经用户明确要求，不执行 `git reset --hard`、`git clean` 或其他可能丢失工作区内容的操作。

# 工作流程

1. **分析**：检查 `git status`、相关 diff 和代码，确认当前状态与约束。
2. **规划**：明确改动范围、兼容性影响和验证方式；复杂任务先拆分步骤。
3. **实现**：仅修改完成需求所需内容，同步必要的测试和文档。
4. **验证**：执行与改动风险匹配的检查：
   - 纯文档修改检查 diff、格式和引用。
   - Rust 修改优先运行 `cargo fmt --all -- --check` 和相关测试。
   - 跨 crate 或公共逻辑修改运行 `cargo test --workspace --all-targets`。
   - Windows 平台、Agent、构建或发布修改按需运行 `.\scripts\build.ps1`。
5. **自检**：复查 diff、验证结果、边界和兼容性，确认未引入敏感信息、构建产物或无关改动。
6. **记录**：仅在原因、影响或实现方式值得长期保留时更新修改记录。
7. **提交**：有文件修改且自检通过后，按下述提交流程处理。

# 跨平台要求

- 默认同时考虑 Windows、Linux 和 macOS；除非需求明确限定平台，不得引入仅在单一平台可用的实现。
- 路径处理使用标准库或跨平台抽象，不硬编码路径分隔符、盘符、临时目录、用户目录或可执行文件后缀。
- 命令、脚本和进程调用不得默认依赖特定 Shell；必须依赖平台命令时，按平台提供等价实现并清晰隔离差异。
- 平台专属 API、依赖和代码使用条件编译或独立模块封装，公共接口与默认行为在各平台保持一致。
- 新增或修改构建配置、依赖、环境变量、文件权限、信号、套接字及进程管理逻辑时，检查各目标平台的兼容性和失败行为。
- 测试应覆盖平台无关逻辑；平台专属逻辑应提供对应测试。无法在当前环境验证的平台，必须进行静态检查并在交付说明中明确未验证项和风险。
- 文档中的命令示例优先提供跨平台写法；无法统一时，同时给出 PowerShell 与 POSIX Shell 示例。

# 修改记录

如无长期保留价值，不额外创建记录文件：

- Bugfix 追加到当天的 `docs/bugfix/YYYY-MM-DD.md`。
- Feature 追加到当天的 `docs/features/YYYY-MM-DD.md`。
- 文件不存在时再创建。
- Issues/TODO：用户提出 Bug 但未明确要求解决时，将该 Bug 以 TODO 形式记录到 `docs/issues/YYYY-MM-DD.md`。

# 提交

- 准备、拆分、生成、审查或执行 Commit 时，必须使用仓库级 `$committing-changes` Skill。
- 当前任务产生文件修改且自检通过后自动提交，无需等待用户确认。
- 用户明确要求不提交，或任务仅涉及审查、分析、答疑、生成 Commit message 时，不创建 Commit。
- 仅提交当前任务的文件或变更块；不得包含 `TODO.md` 等用户未要求的变更。
- 无法安全隔离当前任务变更时，不得提交，并向用户说明原因。
- 所有修改均由主 agent 自检，发现问题后修复并重新验证。
- 自检通过后，由主 agent 按 `$committing-changes` Skill 完成提交。
